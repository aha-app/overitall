{
  "name": "vscode-overitall",
  "displayName": "Overitall",
  "description": "Process management and log viewing for Overitall",
  "version": "0.1.53",
  "publisher": "jemmyw",
  "repository": {
    "type": "git",
    "url": "https://github.com/jemmyw/overitall"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:Procfile",
    "workspaceContains:.oit.sock"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "overitall",
          "title": "Overitall",
          "icon": "$(server-process)"
        }
      ]
    },
    "commands": [
      {
        "command": "overitall.start",
        "title": "Start Overitall",
        "icon": "$(play)"
      },
      {
        "command": "overitall.showTerminal",
        "title": "Show Overitall Terminal",
        "icon": "$(terminal)"
      },
      {
        "command": "overitall.refresh",
        "title": "Refresh Processes",
        "icon": "$(refresh)"
      },
      {
        "command": "overitall.restart",
        "title": "Restart Process",
        "icon": "$(debug-restart)"
      },
      {
        "command": "overitall.stop",
        "title": "Stop Process",
        "icon": "$(debug-stop)"
      }
    ],
    "views": {
      "overitall": [
        {
          "id": "overitallProcesses",
          "name": "Processes"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "overitallProcesses",
        "contents": "Overitall is not running.\n[Start Overitall](command:overitall.start)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "overitall.showTerminal",
          "when": "view == overitallProcesses",
          "group": "navigation@1"
        },
        {
          "command": "overitall.refresh",
          "when": "view == overitallProcesses",
          "group": "navigation@2"
        }
      ],
      "view/item/context": [
        {
          "command": "overitall.restart",
          "when": "view == overitallProcesses && viewItem == process",
          "group": "inline"
        },
        {
          "command": "overitall.stop",
          "when": "view == overitallProcesses && viewItem == process",
          "group": "inline"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.0",
    "typescript": "^5.0.0"
  }
}
