procfile = "example/Procfile"
batch_window_ms = 5000
max_log_buffer_mb = 50
hidden_processes = []
compact_mode = true

[processes.web]
log_file = "web.log"

[processes.worker]
log_file = "worker.log"

[filters]
include = []
exclude = []
